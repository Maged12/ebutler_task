import 'package:flutter/material.dart';

class H100 extends StatelessWidget {
  const H100({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 100,
    );
  }
}