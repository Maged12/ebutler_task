import 'package:flutter/material.dart';

class H75 extends StatelessWidget {
  const H75({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 75,
    );
  }
}