import 'package:flutter/material.dart';

class H25 extends StatelessWidget {
  const H25({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 25,
    );
  }
}